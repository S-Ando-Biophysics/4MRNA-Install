#!/usr/bin/env bash
set -euo pipefail
SCRIPT_PATH="$HOME/4MRNA/4MRNA.sh"
if [[ -x "$SCRIPT_PATH" ]]; then
  exec bash "$SCRIPT_PATH" "$@"
else
  echo "4MRNA.sh not found. Please reinstall." >&2
  exit 1
fi
